/* =====================================================================
   GENERALS: ASHFRONT — прототип базовых механик RTS
   Слой 1 (написан первым ИИ в цепочке). Архитектура рассчитана на то,
   что следующие ИИ будут ДОБАВЛЯТЬ модули, а не переписывать этот файл
   с нуля. См. PROMPT_FOR_NEXT_AI.md рядом с game.html — это тот же
   комментарий-промпт, что раньше лежал внизу game.html, вынесенный в
   отдельный файл при разбивке JS на модули (см. REFACTORING_NOTE.md).

   СТРУКТУРА ФАЙЛОВ (загружаются по порядку в game.html, порядок важен —
   каждый следующий файл использует переменные/функции из предыдущих):
     js/00-header-comment.js   — этот файл
     js/01-config-state.js     — Firebase init, GameConfig, State, UnitDefs, BuildingDefs
     js/02-utils-canvas.js     — canvas/camera, uid/dist/worldToScreen и т.п.
     js/03-pathfinding.js      — A* патфайндинг (ИИ №7)
     js/04-fog-of-war.js       — фог войны (ИИ №6)
     js/05-init-world.js       — logMsg, инициализация игрока и карты
     js/06-enemy-ai.js         — вражеский ИИ (конечный автомат)
     js/07-game-loop-combat.js — gameTick, боевая система, очередь производства
     js/08-render.js           — весь Canvas-рендер + requestAnimationFrame loop
     js/09-input.js            — мышь/клавиатура, выделение, движение
     js/10-hud.js               — HUD: build bar, панель выделения
     js/10b-enemy-select-screen.js — экран выбора сложности/фракции перед стартом
     js/11-networking.js       — Firebase RTDB синхронизация (MVP netcode)
     js/12-ai-advisor.js       — кнопка "Совет ИИ-командующего" (LLM-запрос)
     js/13-bootstrap.js        — init() и запуск игры
     js/13b-battle-stats.js    — ИИ №30: сбор статистики боя (килы/потери/пик
                                  экономики) для экрана победы, патчит
                                  killUnit/killBuilding/renderHUD
     js/14-game-end.js         — ИИ №25/№30: экран победы/поражения с разбором боя
     js/15-difficulty-patch.js — патч баланса сложности ИИ (не интерфейс)
     js/16-tech-unlock.js      — ИИ №28: разблокировка найма юнитов по
                                  построенным зданиям (UI-слой поверх
                                  BuildingDefs.produces), секции build bar
     js/17-hud-polish.js       — ИИ №29: полировка панели выделения
                                  (группировка юнитов, HP-цвет, иконки),
                                  цветные метки игрока/врага в topbar

   ЕСЛИ ДОБАВЛЯЕШЬ НОВЫЙ МОДУЛЬ: создай новый js/NN-название.js, подключи
   его через <script src="js/NN-название.js"></script> в game.html СТРОГО
   в нужном месте общей последовательности (после файлов, чьи функции/
   переменные он использует, до файлов, которые могут понадобиться ему).
   Не создавай ES-модули (import/export) — все файлы работают как обычные
   классические <script>, делят один и тот же глобальный scope, порядок
   подключения = порядок объявления. Это осознанное упрощение, чтобы сайт
   оставался чистым статическим HTML без сборки.
   ===================================================================== */