/* ---------------------------- ИИ №28 → ПЕРЕНЕСЕНО в 10-hud.js (ИИ №30) ---------------------------- */
// ИСТОРИЯ: этот файл изначально (ИИ №28) добавлял разблокировку юнитов по
// зданиям поверх buildBuildBar/renderHUD приёмом "переопределить функцию
// ПОСЛЕ её объявления", не трогая 10-hud.js напрямую — см. историю решений
// в git/старых версиях файла.
//
// ИИ №30: по прямому запросу пользователя ("разделить на 3 кнопки которые
// ведут к 3 классам: техника, здания, юниты(боец)") buildBuildBar
// переписан НАПРЯМУЮ в 10-hud.js — юниты в нём теперь дополнительно
// делятся на "Техника" и "Юниты (бойцы)" вместо одной общей секции
// "Юниты". Вся логика блокировки (unlockedProducerBuildingKeys,
// unitUnlockStatus, refreshUnitLockState, glyph-хелперы) физически
// переехала в 10-hud.js вместе с buildBuildBar, чтобы не держать 3
// категории синхронно в двух местах (здесь и там). Патч в этом файле
// ПОЭТОМУ ОТКЛЮЧЁН — если бы он остался активным, он переопределил бы
// buildBuildBar ПОСЛЕ 10-hud.js и стёр бы категории техники/пехоты
// обратно в одну секцию "Юниты" (тот самый способ, которым сам когда-то
// патчил 10-hud.js).
//
// Файл оставлен в дереве проекта (не удалён) ради истории решений и
// порядка подключения в game.html — можно безопасно убрать
// <script src="js/16-tech-unlock.js"> при следующей уборке, но само
// удаление файла не входит в текущий запрос.
//
// ВАЖНО для след. ИИ: unitUnlockStatus, unlockedProducerBuildingKeys,
// refreshUnitLockState, buildingIconGlyph, unitIconGlyph теперь
// объявлены в 10-hud.js — НЕ дублировать их здесь повторным
// объявлением function с тем же именем (JS тихо перезапишет более
// раннее объявление тем, что подключено позже в <script> порядке,
// поэтому дублирование — не ошибка загрузки, а тихий баг поведения).

(function noop() {
  // Намеренно пусто — весь прежний код патча отключён, см. комментарий выше.
})();
